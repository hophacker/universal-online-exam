１、数据库student表
（１）首先，建表的时候我建议对每个字段加上注释，标注这个字段的作用，方便别的开发人员。
A:in processing
（有个字段命名的建议，对于外键最好能在字段名上加以区分，比如加上fk_这样的前缀，这样可能会使操作数据库的时候变的清楚一点）
A:外键越少越好
（２）表没有设置主键，是否S_ID就是主键。如何实现主键字段的唯一，我觉得可以有两个方案，一个是直接用时间戳，第二个是自己写一个自增的函数，每次插入数据的时候都主动调用一下。
A:done.统一autoincrement，你check一下
（３）S_DATE_YEAR、S_DATE_MONTH、S_DATE_DAY这三个字段有什么用，可否合并为一个字段，用date类型。
A:合并成了S_DATE,datetime类型，默认格式YYYY-MM-DD.之前因为怕引起时间上面的显示冲突。
（４）END_SEC字段有什么作用
A:I don't know now.Let me check.
（５）建议添加几个备注字段，以备不时之需
A:ok
（５）学号应该需要加上索引加快查找的效率
A:索引因为可以随时删除随时添加，现在先不考虑。等真正确定哪些字段非常常用时再弄。
（６）表里是否需要加上角色标志来区分积极分子或是预备党员（针对不同的需求，增加角色字段可以更灵活）
A:yeah good idea. done.
２、数据库department表
（１）里面的PAPER_ID有什么作用。
A:主键，其他可以到这边外键.
３、数据库infoboard表
（１）通知是否需要标注由何部门发出，是否每个部门都能够发通知，还有通知发出之后有哪些角色能够看见
yeah, good idea.
4. 在department表中，那个paper_id的问题我想说的是可能一个学院有多张不同的试卷，
    个人认为不应该把试卷和学院绑定在一起，应该把试卷跟学生绑定在一起
   

